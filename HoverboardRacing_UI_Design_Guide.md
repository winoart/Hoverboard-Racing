# 호버보드 레이싱 UI 디자인 가이드

본 문서는 전달해주신 `InventoryHUD` 버튼(스크린샷)을 기반으로 분석한 **캐주얼/시뮬레이터 스타일(Casual Simulator Style)** UI 디자인 가이드입니다. 
로블록스의 대중적인 캐주얼 게임에서 자주 쓰이는 둥글고 귀여운 카툰 렌더링 테마를 따릅니다.

---

## 1. 전반적인 아트 스타일 (Art Style)
* **테마**: 캐주얼, 카툰, 밝고 경쾌함
* **핵심 특징**: 매우 두꺼운 검은색 외곽선(Stroke), 밝고 쨍한 원색 계열 배경, 입체감을 주는 밝은색 이너 하이라이트(Inner Highlight).

---

## 2. 색상 팔레트 (Color Palette)

전체적으로 눈에 띄고 화사한 밝은 색상을 사용하며, 어두운 톤은 외곽선에만 사용합니다.

| 용도 | 설명 |
| --- | --- |
| **메인 배경 (Primary BG)** | **밝은 하늘색 (Sky Blue)** - 캡쳐된 버튼의 메인 색상. 맑고 팝(Pop)한 느낌. |
| **포인트 색상 (Accent)** | **주황색/갈색 계열** - 아이콘 등에 사용되어 파란 배경과 대비(보색)를 이룸. |
| **외곽선 (Stroke)** | **검은색 (`0, 0, 0`)** - UI의 카툰 느낌을 살려주는 가장 핵심적인 색상. |
| **텍스트 색상** | **순백색 (`255, 255, 255`)** - 두꺼운 검은 외곽선 안에서 가장 잘 보이는 색상. |
| **내부 하이라이트** | 밝은 하늘색(또는 흰색)을 이용해 둥근 입체감을 표현. |

---

## 3. 타이포그래피 (Typography)

글자가 작아도 눈에 확 들어오도록 **굵고 둥글둥글한 폰트**를 사용하며, **두꺼운 외곽선**이 필수적으로 들어갑니다.

* **권장 폰트 (Font Family)**: `FredokaOne`, `LuckiestGuy`, `Cartoon`, 혹은 `GothamBlack`처럼 둥글고 두꺼운 폰트.
* **텍스트 외곽선 (UIStroke)**: 
  * 색상: 검은색 (`0, 0, 0`)
  * 두께(Thickness): 일반 UI보다 훨씬 두꺼운 수치 적용 (예: `3px` ~ `5px` 이상)
* **텍스트 색상**: 하얀색 (`255, 255, 255`)
* **배치 특징**: 하단의 "인벤토리" 텍스트처럼 배경 박스 밖으로 살짝 튀어나오거나, 겹쳐지게 배치하여 역동적인 느낌을 줍니다.

---

## 4. 형태 및 테두리 (Shapes & Borders)

모든 UI 컴포넌트는 매우 둥글둥글하고 통통 튀는 입체적인 느낌을 주어야 합니다.

### 4.1. 모서리 둥글기 (UICorner)
* **버튼 및 메인 패널**: 모서리를 매우 둥글게 처리합니다. (예: `CornerRadius = UDim.new(0, 16)` 이상 또는 `0.2` 등의 Scale 비율 사용)
* 날카롭거나 각진 모서리는 디자인 가이드상 철저히 배제합니다.

### 4.2. 외곽선 및 입체감 (Borders & 3D Effect)
* **두꺼운 검은 테두리 (UIStroke)**: 버튼, 텍스트, 아이콘 등 모든 외곽에 두꺼운 검은색 선(두께 `4px`~`8px`)을 반드시 적용합니다.
* **내부 테두리 (Inner Glow/Highlight)**: 검은 외곽선 바로 안쪽에 얇은 밝은색(하얀색이나 옅은 하늘색) 테두리를 추가해 볼록 튀어나온 젤리 같은 입체감을 줍니다.
* **그림자 (Drop Shadow)**: 버튼 텍스트 아래나 버튼 자체에 약간 아래 방향으로 쳐진 검은 그림자를 넣어 깊이감을 줍니다.

---

## 5. 아이콘 (Icons)
* 복잡하고 사실적인 그래픽(실사 느낌) 대신, 단순화된 **플랫/카툰 스타일의 2D 또는 3D 아이콘**을 사용합니다.
* 아이콘 내부 요소들 역시 부드러운 곡선과 두꺼운 선으로 이루어져야 하며, 전체적인 UI 테마와 이질감이 없도록 합니다. (스크린샷의 가방 아이콘 참고)
