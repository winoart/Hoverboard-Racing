# 호버보드 레이싱 UI 디자인 가이드

본 문서는 호버보드 레이싱의 UI 디자인 원칙을 정리한 가이드입니다.
**'Thick Cartoon Style' (두꺼운 카툰 렌더링 스타일)**을 기본으로 하며, 투명한 **'유리(Glass)' 질감**과 직관적인 **순위 기반 컬러 시스템**을 혼합하여 사용합니다.

---

## 1. 전반적인 아트 스타일 (Art Style)
* **테마**: 캐주얼, 카툰, 직관적임, 투명한 유리 질감(Glass effect)
* **핵심 특징**: 
  * 두꺼운 검은색 외곽선 (UIStroke)
  * 투명도를 주어 뒤쪽 배경(월드)이 은은하게 비치는 메인 패널
  * 직관적인 계층(1등, 2등, 3등) 기반의 컬러 팔레트 사용

---

## 2. 컬러 팔레트 (Color Palette)

UI의 통일성을 위해 게임 내 '순위(Leaderboard)'에서 파생된 색상을 핵심 컬러로 사용합니다.

| 분류 | 색상 코드 (RGB) | 용도 및 설명 |
| --- | --- | --- |
| **타이틀 헤더** | `(40, 180, 255)` | 제목이나 헤더 배경에 쓰이는 쨍한 파란색 |
| **메인 배경 (유리)** | `(150, 240, 255)` | 창의 메인 배경. `BackgroundTransparency = 0.5`를 적용해 유리 질감을 냄 |
| **1위 컬러 (Gold)** | `(255, 200, 50)` | 가장 중요한 정보, 날짜(Day), '오늘'의 보상, 1등 표시 |
| **2위 컬러 (Silver)**| `(210, 220, 230)` | 부가적인 정보, 이미 수령한(Claimed) 카드 배경, 2등 표시 |
| **3위 컬러 (Bronze)**| `(220, 140, 90)` | 재화 보상량 표기, 3등 표시 |
| **성공/액션 버튼** | `(100, 220, 110)` | 수령(Claim) 등 주요 긍정적 상호작용 버튼 (밝은 녹색) |
| **텍스트 색상** | `(30, 30, 30)` | 기본 텍스트 색상은 완전 검정보다 살짝 부드러운 다크 그레이 사용 |
| **외곽선 (Stroke)** | `(0, 0, 0)` | 모든 요소의 두꺼운 테두리에 사용 |

---

### 3. 텍스트 및 타이포그래피 (Typography)
* **주요 폰트**: 
  - 영문: `FredokaOne` (캐주얼하고 둥근 폰트)
  - 한글: `Montserrat` (유저의 강력한 요청에 의해 지정)
* **폰트 굵기(Weight)**: 주요 탭이나 제목은 **`ExtraBold`**를 사용하여 텍스트의 가독성과 무게감을 높임.
* **텍스트 외곽선(Stroke)**: 두께(Thickness) `3px` 적용 (글씨가 배경에 묻히지 않도록 필수 적용).
* **텍스트 색상 규칙**: 
  * 기본 본문 및 타이틀: 다크 그레이 (`30, 30, 30`) + 외곽선 없음 혹은 얇은 흰색
  * 컬러 배경 위 강조 텍스트(예: 탭 메뉴): 하얀색 (`255, 255, 255`) + 두꺼운 검은색 외곽선(`3px`)으로 명확히 구분.

---

## 4. 형태 및 테두리 (Shapes & Borders)

### 4.1. 메인 패널 (Main Panel)
* **둥근 모서리**: `UICorner` Radius `24px` 적용.
* **유리 질감**: `BackgroundColor3 = (150, 240, 255)`, `BackgroundTransparency = 0.5`를 통해 반투명 효과.
* **외곽선**: 검은색 외곽선 `Thickness = 8`로 창 전체를 또렷하게 잡음.

### 4.2. 타이틀 헤더 (Title Header)
* 창 상단에 위치하며, 좌우가 완전히 둥근 형태(알약 모양, `UICorner = 0.5, 0`).
* 파란색 배경(`40, 180, 255`)과 검은색 외곽선(`Thickness = 6`) 사용.

### 4.3. 정보 카드 및 버튼 (Cards & Buttons)
* **카드 코너**: `UICorner` Radius `12px` ~ `16px`.
* **버튼 외곽선**: 검은색 `Thickness = 4` ~ `6`. (활성화 여부에 따라 두께 변형 가능)
* **닫기 버튼 (Close Button)**: 우측 상단 모서리에 튀어나오듯 배치. 완전한 원형(`UICorner = 1, 0`)에 빨간색 배경(`255, 80, 80`) 사용.

---

## 5. 패턴 및 애니메이션 특수 규칙 (Patterns & Animations)

### 5.1 사선 스트라이프 패턴 (Diagonal Stripes)
* 상세 정보창 등 포인트가 되는 영역의 배경에는 반투명한 흰색 사선 스트라이프 패턴을 적용함.
* **구현 방식**: 외부 이미지 에셋(PNG)을 사용하지 않고, 100% **`UIGradient`의 `NumberSequence`**를 이용한 수학적 렌더링으로 구현함. 
  - **이유**: `CanvasGroup`을 사용할 경우 모바일 기기에서 화질이 저하되거나 둥근 모서리(`UICorner`) 바깥으로 클리핑 버그가 발생하기 때문. `UIGradient`는 모든 해상도/비율에서 완벽하게 대응됨.
* **적용법**: 부모 프레임 내부에 `ZIndex = 1`인 하얀색 덧씌움 프레임(PatternBg)을 만들고, `Rotation = 45`, `Transparency = 0.85 ↔ 1`이 교차하는 10줄의 그라데이션을 적용. 텍스트 등은 `ZIndex = 2`로 올려 패턴 위에 오도록 함.

### 5.2 아이템 바운스 애니메이션 (Bounce FX)
* 인벤토리 썸네일 등에서 아이템이 둥둥 떠다니는 애니메이션이 프레임 바깥(외곽선)을 침범해 삐져나가는 것을 방지해야 함.
* **여백(Padding) 확보**: 대상 이미지(`ImageLabel`)의 크기를 부모보다 작게(`Scale = 0.9`) 설정하고, 중심점 위치를 정중앙(`Position = 0.05, 0.05`)으로 잡아 충분한 여백 공간 내에서만 애니메이션이 플레이되도록 제한함.

---

## 6. UI 가이드라인 요약 (디자인 체크리스트)
1. 창 뒤의 게임 월드가 비쳐 보이는가? (유리 질감 적용 확인)
2. 가장 중요한 강조 요소에 '골드' 컬러가 사용되었는가?
3. 모든 UI 요소(패널, 버튼, 텍스트)에 굵은 검은색 테두리(UIStroke)가 있는가?
4. 한글 폰트는 `Montserrat ExtraBold`를 사용했는가?
5. 모든 모서리가 부드럽게 라운드 처리(UICorner) 되어 있는가?
6. 모바일 대응을 위해 CanvasGroup 대신 UIGradient 기반 패턴을 사용했는가?
